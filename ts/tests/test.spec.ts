/// <reference path="/usr/local/lib/node_modules/bobril-build/typings/jasmine/jasmine.d.ts" />
import * as World from '../index';

describe("main",()=>{
    it("Should have something", () => {
        expect(1).toBe(2);
    });
}); 